export function PhonePeMark({ className = "h-4 w-auto" }: { className?: string }) {
  // eslint-disable-next-line @next/next/no-img-element
  return <img src="/phonepe-logo.svg" alt="PhonePe" className={className} />;
}
